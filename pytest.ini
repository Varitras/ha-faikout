[pytest]
testpaths = tests
# pytest-homeassistant-custom-component registers autouse async fixtures.
# Without auto mode pytest-asyncio leaves them unhandled, which pytest >= 9
# turns into an error even for the plain synchronous tests.
asyncio_mode = auto
